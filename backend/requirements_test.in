# Test Dependencies
# Install with: pip install -r requirements.txt -r requirements_test.txt

# Testing Framework
pytest==9.0.3
pytest-django==4.7.0
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.5.0  # Parallel test execution

# Test Data Factories
factory-boy==3.3.0
faker==22.0.0

# HTTP Mocking
responses==0.25.0
requests-mock==1.11.0

# Time Mocking
freezegun==1.5.1  # Updated for Python 3.13 compatibility

# celery itself is pinned in requirements.in. The [pytest] extra used to
# pull in pytest-celery, but no test in the suite imports anything from it,
# so we don't need it (and recent versions clash with our urllib3/responses).

# WebSocket Testing
websockets==12.0

# Code Quality
flake8==7.0.0
black==24.10.0
isort==5.13.2
mypy==1.8.0
