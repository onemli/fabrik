// Home.tsx
//
// The main home page — shows the operational dashboard. Query building is
// handled exclusively at /builder with URL-based navigation.

import { DashboardContent } from '@/components/dashboard'

export default function Home() {
  return <DashboardContent />
}
